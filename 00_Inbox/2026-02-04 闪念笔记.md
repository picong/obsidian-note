# 2026-02-04 闪念笔记

## 场景
- 在做什么：
- 阅读：PV、PVC、StorageClass，这些到底在说啥？
- 阅读：PV、PVC体系是不是多次一举？本地持久化卷谈起
- 阅读：顶层设计：构建你的AI原生开发"驾驶舱"

## 瞬时想法
- volumeMounts中的mountPath是容器内目录，该目录是怎么跟宿主机关联起来的。
- Volume的原理可能要再熟悉一下。
- PV和PVC的绑定在代码层面，其实就是将PV对象的名字填在PVC对象的spec.volumeName字段上，这个逻辑需要后续看k8s源码来证明。
- hostPath和emptyDir在yaml中定义和持久化volume的定义有什么不同之处。
- 持久化volume可以被运维人员手动访问到吗？
- 针对远程快设备的Attach操作，是将远程块设备添加到Pod所在的宿主机，即在`/dev/`在面生成一个块设备。
- kubelet中的每个控制循环是不是都是一个单独的协程？
- AttachDetachController在Master节点上，VolumeManagerReconciler执行mount操作必须跟宿主机相关，所以它必须是kubelet组件的一部分。
- StrorageClass创建出来的PV容量是怎么控制的，目前来看是跟PVC声明的request.storage保持一致。
- k8s中的Addmision Plugin机制是什么？
- Local Persistent Volume的应用对I/O较为敏感，并且必须具备数据备份和恢复的能力。
- Local Persistent Volume只能是额外挂载在宿主机的磁盘或者快设备。
- Kubernetes中的node对象是怎么来的？
- Local Persistent Volume必须推迟绑定，通过在StorageClass对象中声明volumeBindingMode=WaitForFirstConsumer来进行延迟绑定，推迟到调度的时候进行绑定。
- 参考External Provisioner的文档，自己部署一下。
- 规范驱动开发对于我们存量业务比较复杂的项目该怎么应用？
- Claude code启动时会先加载个人配置，然后再加载项目配置，如果冲突，会使用项目配置覆盖个人配置。
- 

## 疑问 / 联想
- 
- 

## 可能有价值的点（不确定）
- 
